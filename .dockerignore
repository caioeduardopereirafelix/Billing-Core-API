# Build output
target/
build/

# VCS & IDE
.git/
.gitignore
.gitattributes
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# Maven wrapper (the build stage uses the maven base image's own mvn)
.mvn/
mvnw
mvnw.cmd

# Local-only secret files - must never be baked into the image
src/main/resources/application-secrets.yaml
.env
.env.prod

# Not part of the app image
frontend/
deploy/

# Docs, compose, docker files
*.md
docker-compose*.yaml
Dockerfile
.dockerignore
